<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>dev.mikko</groupId>
  <artifactId>scc-check</artifactId>
  <version>0.1.0</version>
  <packaging>jar</packaging>

  <name>scc-check</name>
  <description>Offline checker for Spring Cloud Config CVE-2026-40982 — tells you whether your version line has an OSS fix at all.</description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <!-- 运行时零依赖,是刻意的:排查工具要能丢进任何环境直接跑。 -->
  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- 🔴 插件版本被本机 Maven 3.5.4(2018) 卡死,别升:
           · compiler 3.1(Maven 自带)不认 release,会退回 source 5 编译失败
           · compiler 3.13 / surefire 3.x / jar 3.4 都要求 Maven >= 3.6.3
           下面这组是「支持 release 参数」且「Maven 3.5 能跑」的交集。 -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.2.0</version>
        <configuration>
          <archive>
            <manifest>
              <mainClass>dev.mikko.scccheck.Main</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>2.22.2</version>
      </plugin>
    </plugins>
  </build>
</project>
